all: installed.c++

INSTALL_DIR = `rospack find openrtm`
FILENAME    = OpenRTM-aist-1.1.0-RELEASE.tar.gz
TARBALL     = build/$(FILENAME)
TARBALL_URL = http://www.openrtm.org/pub/OpenRTM-aist/cxx/1.1.0/$(FILENAME)
SOURCE_DIR  = build/OpenRTM-aist-1.1.0
TARBALL_PATCH = patch/SDOPackage.idl-1.1.0.patch patch/Manager.cpp.patch2 patch/ArtEC-1.1.0.patch build/openrtm-bin-1.1.0.patch patch/skel_wrapper_use_md5.patch
UNPACK_CMD  = tar xzf
MD5SUM_FILE = $(FILENAME).md5sum
include $(shell rospack find mk)/download_unpack_build.mk

installed.c++: $(SOURCE_DIR)/unpacked
	cd $(SOURCE_DIR) && ./configure --enable-debug --prefix=$(CURDIR) && make && make install
	touch installed.c++

clean:
	-cd $(SOURCE_DIR) && make clean

wipe: clean
	rm -fr installed.c++
