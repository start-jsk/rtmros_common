# Travis Continuous Integration Configuration File, based on ..
#   Generic MoveIt Travis Continuous Integration Configuration File
#   Works with all MoveIt! repositories/branches
#   Author: Dave Coleman, Jonathan Bohren
language:
  - cpp
  - python
python:
  - "2.7"
compiler:
  - gcc
env:
  - ROS_DISTRO=groovy
  - ROS_DISTRO=hydro
notifications:
  email:
    recipients:
      - k-okada@jsk.t.u-tokyo.ac.jp
      - iisaito@opensource-robotics.tokyo.jp
    on_success: always #[always|never|change] # default: change
    on_failure: always #[always|never|change] # default: always
before_install: # Use this to prepare the system to install prerequisites or dependencies
  # Define some config vars
  - export CI_SOURCE_PATH=$(pwd)
  - export REPOSITORY_NAME=${PWD##*/}
  - echo "Testing branch $TRAVIS_BRANCH of $REPOSITORY_NAME"
  - sudo sh -c 'echo "deb http://packages.ros.org/ros/ubuntu precise main" > /etc/apt/sources.list.d/ros-latest.list'
  - wget http://packages.ros.org/ros.key -O - | sudo apt-key add -
  - sudo apt-get update -qq
  - sudo apt-get install -qq -y python-catkin-pkg python-rosdep python-wstool ros-$ROS_DISTRO-catkin ros-$ROS_DISTRO-ros
  # Setup rosdep
  - sudo rosdep init
  - rosdep update
install: # Use this to install any prerequisites or dependencies necessary to run your build
  # Create workspace
  - mkdir -p ~/ros/ws_$REPOSITORY_NAME/src
  - cd ~/ros/ws_$REPOSITORY_NAME/src
  # BEGIN to use fixed  version
  - wstool init .
  - wstool set openrtm_aist_core https://github.com/start-jsk/openrtm_aist_core --git -y
  - wstool set openhrp3 https://github.com/start-jsk/openhrp3 --git -y
  - wstool set hrpsys https://github.com/start-jsk/hrpsys --git -y
  - wstool set jsk-ros-pkg/jsk_model_tools https://github.com/jsk-ros-pkg/jsk_model_tools --git -y
  - wstool update
  # END
  - ln -s $CI_SOURCE_PATH . # Link the repo we are testing to the new workspace
  - cd ../
  # Install dependencies for source repos
  - rosdep install -r -n --from-paths src --ignore-src --rosdistro $ROS_DISTRO -y
before_script: # Use this to prepare your build for testing e.g. copy database configurations, environment variables, etc.
  - source /opt/ros/$ROS_DISTRO/setup.bash
  - sudo /etc/init.d/omniorb4-nameserver restart
script: # All commands must exit with code 0 on success. Anything else is considered failure.
  - find -L . \! -path "*/.*" -type f | xargs egrep -i "(hoge|fuga)" ; if [ $? == 0 ]; then exit 1; fi
  - catkin_make -j2
  - catkin_make test --pkg hrpsys_ros_bridge hrpsys_tools openrtm_ros_bridge openrtm_tools rosnode_rtc rtmbuild rtmros_common
  - source devel/setup.bash
  - rostest -t hrpsys_tools test-pa10.test
  - (cd `rospack find hrpsys_ros_bridge`/test/test_compile_robot_model; rm -rf models; mkdir -p build && cd build && cmake .. -DHRPSYS_TOOLS_SOURCE_DIR=`rospack find hrpsys_tools` && make)
  - catkin_make install
  - rm -fr devel src build
  - source install/setup.bash
  - find install -iname "*.test" -print0 | xargs -0 -n1 rostest
after_failure:
  - cat ./build/Testing/Temporary/LastTest.log
  - find ${HOME}/.ros/test_results -type f -exec echo "== {} ==" \; -exec cat {} \;
  - for file in ${HOME}/.ros/log/rostest-*; do echo "=== $file ==="; cat $file; done
